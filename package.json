{
  "name": "ths-navigation-page",
  "version": "1.0.0",
  "private": true,
  "description": "同花顺策略导航合集的 Cloudflare Pages 静态部署项目。",
  "scripts": {
    "build": "node -e \"console.log('Static site: no build step required')\"",
    "preview": "npx --yes wrangler pages dev public",
    "pages:deploy": "npx --yes wrangler pages deploy public",
    "deploy": "npx --yes wrangler pages deploy public"
  }
}
